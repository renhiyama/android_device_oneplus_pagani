diff --git a/res/values/evolution_strings.xml b/res/values/evolution_strings.xml
--- a/res/values/evolution_strings.xml
+++ b/res/values/evolution_strings.xml
@@ -425,4 +425,8 @@
     <!-- Per-app GPU composition -->
     <string name="use_gpu_for_screen_composition_title">Use GPU for screen composition</string>
     <string name="use_gpu_for_screen_composition_summary">Select apps that should disable HW overlays while in use</string>
+
+    <!-- Battery: show real charging power on lock screen toggle [CHAR LIMIT=40] -->
+    <string name="show_real_charging_power_title">Show real charging power</string>
+    <string name="show_real_charging_power_summary">Show how much power your phone is actually drawing on the lock screen, instead of the charger\'s maximum capability.</string>
 </resources>
diff --git a/res/xml/power_usage_summary.xml b/res/xml/power_usage_summary.xml
--- a/res/xml/power_usage_summary.xml
+++ b/res/xml/power_usage_summary.xml
@@ -73,6 +73,14 @@
             settings:requiresConfig="@*lineageos.platform:integer/config_deviceLightCapabilities"
             settings:requiresConfigMask="64" />
 
+        <!-- Show real charging power on lock screen instead of the
+             charger's rated capability. Default off (matches AOSP). -->
+        <lineageos.preference.LineageSystemSettingSwitchPreference
+            android:key="show_real_charging_power"
+            android:title="@string/show_real_charging_power_title"
+            android:summary="@string/show_real_charging_power_summary"
+            android:defaultValue="false" />
+
         <Preference
             android:key="idle_manager"
             android:title="@string/idle_manager_title"
