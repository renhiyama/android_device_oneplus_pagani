diff --git a/lineage/lib/main/java/org/lineageos/platform/internal/health/ChargingControlController.java b/lineage/lib/main/java/org/lineageos/platform/internal/health/ChargingControlController.java
index 91a5bdf..2724c8a 100644
--- a/lineage/lib/main/java/org/lineageos/platform/internal/health/ChargingControlController.java
+++ b/lineage/lib/main/java/org/lineageos/platform/internal/health/ChargingControlController.java
@@ -65,6 +65,18 @@ public class ChargingControlController extends LineageHealthFeature {
             LineageSettings.System.CHARGING_CONTROL_START_TIME);
     private final Uri TARGET_TIME_URI = LineageSettings.System.getUriFor(
             LineageSettings.System.CHARGING_CONTROL_TARGET_TIME);
+    private final Uri BYPASS_URI = LineageSettings.System.getUriFor(
+            LineageSettings.System.BYPASS_CHARGING_ENABLED);
+    private final Uri TRUE_BYPASS_URI = LineageSettings.System.getUriFor(
+            LineageSettings.System.TRUE_BYPASS_CHARGING_ENABLED);
+
+    /**
+     * Below this SoC, both Bypass modes are paused so the phone can charge
+     * normally. Above it: user toggles apply.
+     *   - Normal Bypass: auto-resumes when SoC climbs back above the threshold
+     *   - True Bypass:   stays off after auto-pause; user must re-enable
+     */
+    private static final int BYPASS_LOW_BATTERY_THRESHOLD = 20;
 
     // Internal state
     private float mBatteryPct;
@@ -235,6 +247,251 @@ public class ChargingControlController extends LineageHealthFeature {
                 && setStartTime(mDefaultStartTime) && setTargetTime(mDefaultTargetTime);
     }
 
+    /**
+     * Manual bypass-charging override. Independent of CHARGING_CONTROL_ENABLED:
+     * when this is true, the controller forces the HAL into bypass regardless of
+     * the regular Auto/Limit/Manual provider logic.
+     */
+    public boolean isBypassChargingEnabled() {
+        return LineageSettings.System.getInt(mContentResolver,
+                LineageSettings.System.BYPASS_CHARGING_ENABLED, 0) != 0;
+    }
+
+    public boolean setBypassChargingEnabled(boolean enabled) {
+        putBoolean(LineageSettings.System.BYPASS_CHARGING_ENABLED, enabled);
+        return true;
+    }
+
+    /**
+     * Whether the underlying HAL supports bypass mode (battery FET open while
+     * USB powers the system). Used by the UI to show/hide the manual toggle.
+     */
+    public boolean isBypassChargingSupported() {
+        return isChargingModeSupported(
+                vendor.lineage.health.ChargingControlSupportedMode.BYPASS);
+    }
+
+    // True Bypass — fully opens the battery FET via OPLUS Smart Charge
+    // Protection (flag 14). Layered on top of normal Bypass: requires it
+    // to be enabled, and auto-pauses below the safety threshold.
+
+    public boolean isTrueBypassChargingSupported() {
+        // Same precondition as Normal Bypass — both rely on the OPLUS HAL.
+        return isBypassChargingSupported();
+    }
+
+    public boolean isTrueBypassChargingEnabled() {
+        return LineageSettings.System.getInt(mContentResolver,
+                LineageSettings.System.TRUE_BYPASS_CHARGING_ENABLED, 0) != 0;
+    }
+
+    public boolean setTrueBypassChargingEnabled(boolean enabled) {
+        putBoolean(LineageSettings.System.TRUE_BYPASS_CHARGING_ENABLED, enabled);
+        return true;
+    }
+
+    /**
+     * True iff: user enabled BOTH normal Bypass and True Bypass, USB is online,
+     * and SoC is above the safety threshold. Reflects ground-truth engagement.
+     */
+    public boolean isTrueBypassChargingActive() {
+        return isBypassChargingEnabled()
+                && isTrueBypassChargingEnabled()
+                && mIsPowerConnected
+                && mBatteryPct > BYPASS_LOW_BATTERY_THRESHOLD;
+    }
+
+    /** Tracks the last True Bypass smart-limit level we wrote, so we know
+     *  whether to release on the next tick. */
+    private int mLastTrueBypassLevel = -1;
+
+    /**
+     * Reconcile True Bypass engagement with current state. Engages OPLUS
+     * smart-limit at max(currentPct, 20) when all preconditions are met
+     * (Normal Bypass on, True Bypass on, USB plugged, SoC > 20%); auto-
+     * disables the user setting when SoC hits 20%; releases smart-limit
+     * + notification when not engaged. Idempotent — safe to call every
+     * updateChargeControl tick.
+     */
+    private void updateTrueBypassEngagement() {
+        final boolean userWants = isBypassChargingEnabled() && isTrueBypassChargingEnabled();
+        final boolean safeSoC = mBatteryPct > BYPASS_LOW_BATTERY_THRESHOLD;
+        final boolean shouldEngage = userWants && mIsPowerConnected && safeSoC;
+
+        if (shouldEngage) {
+            int level = Math.max((int) Math.floor(mBatteryPct),
+                    BYPASS_LOW_BATTERY_THRESHOLD);
+            if (level != mLastTrueBypassLevel) {
+                if (Util.callOplusSmartLimit(true, level)) {
+                    mLastTrueBypassLevel = level;
+                }
+            }
+            postTrueBypassNotification(level);
+        } else {
+            // Release smart-limit if previously engaged.
+            if (mLastTrueBypassLevel != -1) {
+                Util.callOplusSmartLimit(false, 100);
+                mLastTrueBypassLevel = -1;
+            }
+            // If user wanted it but SoC dropped to threshold, auto-disable
+            // and post a transient "low battery, reverted" notification.
+            if (userWants && mIsPowerConnected && !safeSoC) {
+                Log.i(TAG, "True Bypass auto-disabled: SoC " + mBatteryPct
+                        + " ≤ " + BYPASS_LOW_BATTERY_THRESHOLD);
+                setTrueBypassChargingEnabled(false);
+                postTrueBypassRevertNotification();
+            } else {
+                cancelTrueBypassNotification();
+            }
+        }
+    }
+
+    private static final int TRUE_BYPASS_NOTIFICATION_ID = 0xCC0F;
+    private static final String TRUE_BYPASS_NOTIFICATION_CHANNEL =
+            "lineage_health_true_bypass";
+    private boolean mTrueBypassChannelCreated = false;
+
+    private void ensureTrueBypassChannel(android.app.NotificationManager nm) {
+        if (mTrueBypassChannelCreated) return;
+        android.app.NotificationChannel ch = new android.app.NotificationChannel(
+                TRUE_BYPASS_NOTIFICATION_CHANNEL,
+                "True Bypass Charging",
+                android.app.NotificationManager.IMPORTANCE_LOW);
+        ch.setDescription(
+                "Active when True Bypass is engaged. Hide this category to "
+                + "suppress these notifications.");
+        ch.setShowBadge(false);
+        nm.createNotificationChannel(ch);
+        mTrueBypassChannelCreated = true;
+    }
+
+    private android.app.PendingIntent disableTrueBypassIntent() {
+        Intent intent = new Intent(
+                "org.lineageos.platform.health.action.DISABLE_TRUE_BYPASS");
+        intent.setPackage(mContext.getPackageName());
+        return android.app.PendingIntent.getBroadcast(mContext, 0, intent,
+                android.app.PendingIntent.FLAG_UPDATE_CURRENT
+                        | android.app.PendingIntent.FLAG_IMMUTABLE);
+    }
+
+    private void postTrueBypassNotification(int level) {
+        android.app.NotificationManager nm =
+                mContext.getSystemService(android.app.NotificationManager.class);
+        if (nm == null) return;
+        ensureTrueBypassChannel(nm);
+        android.app.Notification n = new android.app.Notification.Builder(
+                mContext, TRUE_BYPASS_NOTIFICATION_CHANNEL)
+                .setSmallIcon(android.R.drawable.ic_lock_idle_charging)
+                .setContentTitle("True bypass charging is on")
+                .setContentText("Your phone is using power from the charger "
+                        + "without using your battery. Will switch to normal "
+                        + "bypass charging at "
+                        + BYPASS_LOW_BATTERY_THRESHOLD + "% battery.")
+                .setStyle(new android.app.Notification.BigTextStyle().bigText(
+                        "Your phone is using power from the charger only, so "
+                        + "your battery does not charge or drain. This reduces "
+                        + "battery wear. True bypass will turn off "
+                        + "automatically when the battery reaches "
+                        + BYPASS_LOW_BATTERY_THRESHOLD + "%, and normal "
+                        + "bypass charging will take over."))
+                .setOngoing(true)
+                .setShowWhen(false)
+                .addAction(new android.app.Notification.Action.Builder(
+                        android.R.drawable.ic_menu_close_clear_cancel,
+                        "Disable", disableTrueBypassIntent()).build())
+                .build();
+        nm.notify(TRUE_BYPASS_NOTIFICATION_ID, n);
+    }
+
+    private void postTrueBypassRevertNotification() {
+        android.app.NotificationManager nm =
+                mContext.getSystemService(android.app.NotificationManager.class);
+        if (nm == null) return;
+        ensureTrueBypassChannel(nm);
+        android.app.Notification n = new android.app.Notification.Builder(
+                mContext, TRUE_BYPASS_NOTIFICATION_CHANNEL)
+                .setSmallIcon(android.R.drawable.ic_lock_idle_charging)
+                .setContentTitle("True bypass charging turned off")
+                .setContentText("Your battery reached "
+                        + BYPASS_LOW_BATTERY_THRESHOLD
+                        + "%. Normal bypass charging is now in use. You can "
+                        + "turn true bypass back on from Settings later.")
+                .setAutoCancel(true)
+                .build();
+        nm.notify(TRUE_BYPASS_NOTIFICATION_ID, n);
+    }
+
+    private void cancelTrueBypassNotification() {
+        android.app.NotificationManager nm =
+                mContext.getSystemService(android.app.NotificationManager.class);
+        if (nm != null) {
+            nm.cancel(TRUE_BYPASS_NOTIFICATION_ID);
+        }
+    }
+
+    private boolean writeBypassToHal(boolean bypassActive) {
+        // The Lineage charging-control HAL writes to /sys/class/oplus_chg/common/plc
+        // (PLC = "Plain Loop Charging" voter). On this device that node accepts
+        // writes but doesn't actually open the battery FET — verified empirically.
+        // The only call path the kernel honours is OPLUS's vendor charger AIDL.
+        // We invoke that here as the primary signal AND keep the legacy HAL call
+        // as a non-fatal redundancy (in case the OPLUS HAL is ever down).
+        boolean ok = callOplusBypassAidl(bypassActive);
+
+        if (mChargingControl != null) {
+            try {
+                Log.i(TAG, "writeBypassToHal(" + bypassActive + ") → HAL.setChargingEnabled("
+                        + (!bypassActive) + ")");
+                mChargingControl.setChargingEnabled(!bypassActive);
+            } catch (RemoteException | RuntimeException e) {
+                // UnsupportedOperationException at boot is normal — PLC may not be
+                // loaded yet. Don't crash system_server.
+                Log.w(TAG, "Lineage HAL setChargingEnabled threw, ignoring: "
+                        + e.getMessage());
+            }
+        }
+
+        Log.i(TAG, "writeBypassToHal(" + bypassActive + ") OPLUS=" + ok);
+        return ok;
+    }
+
+    /**
+     * Engage real OPLUS bypass via the V10 vendor charger AIDL.
+     * setChgConfig(flag=25, "1+switch=1"|"1+switch=0", caller=2) — same call OOS
+     * Battery.apk's BypassChargeController makes. Hand-rolled Parcel to avoid
+     * pulling in the OEM-versioned (127-method) Java AIDL stub.
+     */
+    private boolean callOplusBypassAidl(boolean enable) {
+        android.os.IBinder binder = android.os.ServiceManager.getService(
+                "vendor.oplus.hardware.charger.ICharger/default");
+        if (binder == null) {
+            Log.w(TAG, "OPLUS charger HAL not available");
+            return false;
+        }
+        android.os.Parcel data = android.os.Parcel.obtain();
+        android.os.Parcel reply = android.os.Parcel.obtain();
+        try {
+            data.writeInterfaceToken("vendor.oplus.hardware.charger.ICharger");
+            data.writeInt(25);                                   // ChgFuncFlag = bypass
+            data.writeString(enable ? "1+switch=1" : "1+switch=0");
+            data.writeInt(2);                                    // caller = matches OOS
+            if (!binder.transact(127, data, reply, 0)) {        // TRANSACTION_setChgConfig
+                Log.e(TAG, "OPLUS setChgConfig transact returned false");
+                return false;
+            }
+            reply.readException();
+            int result = reply.readInt();
+            Log.i(TAG, "OPLUS setChgConfig(25, " + (enable ? "ON" : "OFF") + ", 2) = " + result);
+            return result == 0;
+        } catch (android.os.RemoteException | RuntimeException e) {
+            Log.e(TAG, "OPLUS setChgConfig failed", e);
+            return false;
+        } finally {
+            reply.recycle();
+            data.recycle();
+        }
+    }
+
     private void updateBatteryInfo(Intent intent) {
         int battStatus = intent.getIntExtra(BatteryManager.EXTRA_STATUS, -1);
         int battPlugged = intent.getIntExtra(BatteryManager.EXTRA_PLUGGED, 0);
@@ -279,11 +536,27 @@ public class ChargingControlController extends LineageHealthFeature {
         }
 
         // Register setting observer
-        registerSettings(MODE_URI, LIMIT_URI, ENABLED_URI, START_TIME_URI, TARGET_TIME_URI);
+        registerSettings(MODE_URI, LIMIT_URI, ENABLED_URI, START_TIME_URI, TARGET_TIME_URI,
+                BYPASS_URI, TRUE_BYPASS_URI);
+
+        // Listen for the True Bypass notification's "Disable" action.
+        IntentFilter f = new IntentFilter(
+                "org.lineageos.platform.health.action.DISABLE_TRUE_BYPASS");
+        mContext.registerReceiver(new BroadcastReceiver() {
+            @Override
+            public void onReceive(Context context, Intent intent) {
+                Log.i(TAG, "True Bypass disable action received from notification");
+                setTrueBypassChargingEnabled(false);
+                updateChargeControl();
+            }
+        }, f, Context.RECEIVER_NOT_EXPORTED);
 
         handleSettingChange();
     }
 
+    /** Track last bypass state so we know when to release the FET. */
+    private boolean mLastBypassWritten = false;
+
     public boolean isChargingModeSupported(int mode) {
         try {
             return isSupported() && (mChargingControl.getSupportedMode() & mode) != 0;
@@ -338,6 +611,11 @@ public class ChargingControlController extends LineageHealthFeature {
         }
         IntentFilter battFilter = new IntentFilter(Intent.ACTION_BATTERY_CHANGED);
         mContext.registerReceiver(mBattReceiver, battFilter);
+        // Refresh battery snapshot + re-evaluate immediately so True Bypass
+        // engages (and its notification posts) on plug-in without waiting
+        // for the first BATTERY_CHANGED broadcast — which can be seconds late.
+        updateBatteryInfo();
+        updateChargeControl();
     }
 
     private void onPowerDisconnected() {
@@ -346,13 +624,18 @@ public class ChargingControlController extends LineageHealthFeature {
             mBattReceiver = null;
         }
 
+        // PLC state is naturally cleared when power is gone, so on next
+        // power-connect we want updateChargeControl to write fresh state.
+        mLastBypassWritten = false;
+
         // On disconnected, reset internal state
         resetInternalState();
     }
 
     private void onPowerStatus(boolean enable) {
-        // Don't do anything if it is not enabled
-        if (!isEnabled()) {
+        // Bypass needs to react to power events too even if regular Charging
+        // Control is off, so don't short-circuit when only bypass is on.
+        if (!isEnabled() && !isBypassChargingEnabled()) {
             return;
         }
 
@@ -420,9 +703,46 @@ public class ChargingControlController extends LineageHealthFeature {
 
     protected void updateChargeControl() {
         if (mCurrentProvider == null) {
+            Log.w(TAG, "updateChargeControl: mCurrentProvider=null, skipping");
             return;
         }
 
+        // Manual bypass override wins over everything. The user explicitly
+        // asked to bypass, so skip the provider state machine entirely.
+        final boolean wantBypass = isBypassChargingEnabled() && mIsPowerConnected;
+        Log.i(TAG, "updateChargeControl: wantBypass=" + wantBypass
+                + " (bypassEnabled=" + isBypassChargingEnabled()
+                + ", powerConnected=" + mIsPowerConnected
+                + ", lastBypassWritten=" + mLastBypassWritten
+                + ", isEnabled=" + isEnabled()
+                + ", mode=" + getMode() + ", limit=" + getLimit()
+                + ", batteryPct=" + mBatteryPct + ")");
+        if (wantBypass) {
+            mCurrentProvider.disable();
+            mChargingNotification.cancel();
+            writeBypassToHal(true);
+            mLastBypassWritten = true;
+            // Maybe also engage True Bypass on top.
+            updateTrueBypassEngagement();
+            return;
+        }
+        // Bypass was previously active and just got turned off — release the
+        // battery FET explicitly so charging can resume. (If Charging Control
+        // is also enabled, its provider will immediately re-evaluate below.)
+        if (mLastBypassWritten) {
+            writeBypassToHal(false);
+            mLastBypassWritten = false;
+        }
+        // Only auto-disable True Bypass when the user actually turned Normal
+        // Bypass off — NOT when the charger merely disconnected. Settings
+        // should persist user intent across plug/unplug cycles.
+        if (!isBypassChargingEnabled() && isTrueBypassChargingEnabled()) {
+            Log.i(TAG, "True Bypass auto-disabled: Normal Bypass turned off");
+            setTrueBypassChargingEnabled(false);
+        }
+        // Release any active smart-limit + clear notification.
+        updateTrueBypassEngagement();
+
         if (!isEnabled() || mIsControlCancelledOnce || !mIsPowerConnected) {
             mCurrentProvider.disable();
             mChargingNotification.cancel();
@@ -492,8 +812,12 @@ public class ChargingControlController extends LineageHealthFeature {
     private void handleSettingChange() {
         int mode = getMode();
 
-        if (mIsEnabled != isEnabled()) {
-            mIsEnabled = isEnabled();
+        // Keep monitoring battery while EITHER Charging Control or manual bypass is on.
+        // Bypass needs power-connect events to write the PLC state and disconnect events
+        // to clear it.
+        final boolean shouldMonitor = isEnabled() || isBypassChargingEnabled();
+        if (mIsEnabled != shouldMonitor) {
+            mIsEnabled = shouldMonitor;
 
             if (mIsEnabled) {
                 if (mBattReceiver == null) {
@@ -503,13 +827,13 @@ public class ChargingControlController extends LineageHealthFeature {
                 }
                 IntentFilter battFilter = new IntentFilter(Intent.ACTION_BATTERY_CHANGED);
                 mContext.registerReceiver(mBattReceiver, battFilter);
-                Log.i(TAG, "Enabled charging control, start monitoring battery");
+                Log.i(TAG, "Enabled charging-state monitoring");
             } else {
                 if (mBattReceiver != null) {
                     mContext.unregisterReceiver(mBattReceiver);
                     mBattReceiver = null;
                 }
-                Log.i(TAG, "Disabled charging control, stop monitoring battery");
+                Log.i(TAG, "Stopped charging-state monitoring");
             }
         }
 
diff --git a/lineage/lib/main/java/org/lineageos/platform/internal/health/HealthInterfaceService.java b/lineage/lib/main/java/org/lineageos/platform/internal/health/HealthInterfaceService.java
index 0431c3e..7391d67 100644
--- a/lineage/lib/main/java/org/lineageos/platform/internal/health/HealthInterfaceService.java
+++ b/lineage/lib/main/java/org/lineageos/platform/internal/health/HealthInterfaceService.java
@@ -39,6 +39,10 @@ public class HealthInterfaceService extends LineageSystemService {
     private ChargingControlController mCCC;
     private FastChargeController mFCC;
 
+    // Tracks the last successful bypass-toggle direction so isBypassChargingActive()
+    // can answer truthfully (Android's bat_status is too cached to use).
+    private static volatile boolean sLastBypassEnabled = false;
+
     public HealthInterfaceService(Context context) {
         super(context);
         mContext = context;
@@ -161,6 +165,186 @@ public class HealthInterfaceService extends LineageSystemService {
                     || mCCC.isChargingModeSupported(ChargingControlSupportedMode.LIMIT);
         }
 
+        @Override
+        public boolean isBypassChargingSupported() {
+            return mCCC != null && mCCC.isBypassChargingSupported();
+        }
+
+        @Override
+        public boolean getBypassChargingEnabled() {
+            return mCCC != null && mCCC.isBypassChargingEnabled();
+        }
+
+        @Override
+        public boolean setBypassChargingEnabled(boolean enabled) {
+            // Persist user intent so it survives reboot + so other components
+            // (AOD label, QS tile) can poll the desired state.
+            if (mCCC != null) {
+                mCCC.setBypassChargingEnabled(enabled);
+            }
+            // Engage real bypass via the OPLUS vendor charger HAL. Direct sysfs
+            // writes (PLC, mmi_charging_enable, chg_path) all fail to actually
+            // open the battery FET — only this binder path does, because the
+            // V10 service coordinates the charge-pump + FET + USB negotiation
+            // atomically. Reverse-engineered from /odm/bin/hw/vendor.oplus.
+            // hardware.charger-V10-service + Battery.apk's BypassChargeController:
+            // setChgConfig(flag=25, "1+switch=1"|"1+switch=0", caller=2).
+            boolean ok = callOplusSetChgConfig(25,
+                    enabled ? "1+switch=1" : "1+switch=0", 2);
+            if (ok) {
+                sLastBypassEnabled = enabled;
+            }
+            return ok;
+        }
+
+        @Override
+        public boolean isBypassChargingActive() {
+            // User-intent + USB-online is more reliable than tracking AIDL
+            // call success across reboots. LineageSettings persists; the
+            // sLastBypassEnabled cache resets to false on every system_server
+            // restart and would make the AOD label silently disappear after
+            // a reboot until the user toggled bypass off and on again.
+            if (mCCC == null || !mCCC.isBypassChargingEnabled()) {
+                return false;
+            }
+            try (java.io.BufferedReader r = new java.io.BufferedReader(
+                    new java.io.FileReader(
+                            "/sys/class/power_supply/usb/online"))) {
+                String s = r.readLine();
+                return s != null && "1".equals(s.trim());
+            } catch (java.io.IOException e) {
+                return false;
+            }
+        }
+
+        /**
+         * Raw-Parcel binder call into vendor.oplus.hardware.charger.ICharger.
+         * We avoid pulling in the Java AIDL stub (127 methods, OEM-versioned)
+         * by hand-rolling the Parcel for setChgConfig (transaction code 127).
+         */
+        private boolean callOplusSetChgConfig(int flag, String extra, int caller) {
+            android.os.IBinder binder = android.os.ServiceManager.getService(
+                    "vendor.oplus.hardware.charger.ICharger/default");
+            if (binder == null) {
+                Log.w(TAG, "OPLUS charger HAL not available");
+                return false;
+            }
+            android.os.Parcel data = android.os.Parcel.obtain();
+            android.os.Parcel reply = android.os.Parcel.obtain();
+            try {
+                data.writeInterfaceToken(
+                        "vendor.oplus.hardware.charger.ICharger");
+                data.writeInt(flag);
+                data.writeString(extra);
+                data.writeInt(caller);
+                // 127 = TRANSACTION_setChgConfig in the V10 ICharger AIDL.
+                if (!binder.transact(127, data, reply, 0)) {
+                    Log.e(TAG, "setChgConfig transact returned false");
+                    return false;
+                }
+                reply.readException();
+                int result = reply.readInt();
+                Log.i(TAG, "OPLUS setChgConfig(" + flag + ", " + extra
+                        + ", " + caller + ") = " + result);
+                return result == 0;
+            } catch (android.os.RemoteException | RuntimeException e) {
+                Log.e(TAG, "OPLUS setChgConfig failed", e);
+                return false;
+            } finally {
+                reply.recycle();
+                data.recycle();
+            }
+        }
+
+        @Override
+        public boolean isTrueBypassChargingSupported() {
+            return mCCC != null && mCCC.isTrueBypassChargingSupported();
+        }
+
+        @Override
+        public boolean getTrueBypassChargingEnabled() {
+            return mCCC != null && mCCC.isTrueBypassChargingEnabled();
+        }
+
+        @Override
+        public boolean setTrueBypassChargingEnabled(boolean enabled) {
+            return mCCC != null && mCCC.setTrueBypassChargingEnabled(enabled);
+        }
+
+        @Override
+        public boolean isTrueBypassChargingActive() {
+            return mCCC != null && mCCC.isTrueBypassChargingActive();
+        }
+
+        @Override
+        public int getRealChargingPowerMicrowatts() {
+            // OPLUS gauge MMS interface — the only source for fresh battery
+            // current that responds to bypass toggles. The standard
+            // power_supply/battery/{current_now,power_now} on this device
+            // are populated by our kernel patch from cpa_get_actual_used_power
+            // (= adapter rated capability), so they don't reflect actual
+            // flow. Reading the gauge requires writing item_id=5 first.
+            try {
+                java.io.File itemId =
+                        new java.io.File("/sys/class/oplus_mms/gauge/item_id");
+                java.io.File data =
+                        new java.io.File("/sys/class/oplus_mms/gauge/data");
+                java.io.File volt = new java.io.File(
+                        "/sys/class/power_supply/battery/voltage_now");
+                if (!itemId.exists() || !data.exists() || !volt.exists()) {
+                    Log.w(TAG, "getRealChargingPower: a node is missing");
+                    return 0;
+                }
+                try (java.io.FileWriter w = new java.io.FileWriter(itemId)) {
+                    w.write("5");
+                } catch (Exception e) {
+                    Log.e(TAG, "getRealChargingPower: write item_id failed", e);
+                    return 0;
+                }
+                int current_mA;
+                try (java.io.BufferedReader r = new java.io.BufferedReader(
+                        new java.io.FileReader(data))) {
+                    String s = r.readLine();
+                    if (s == null) {
+                        Log.w(TAG, "getRealChargingPower: data read null");
+                        return 0;
+                    }
+                    current_mA = Integer.parseInt(s.trim());
+                } catch (Exception e) {
+                    Log.e(TAG, "getRealChargingPower: read data failed", e);
+                    return 0;
+                }
+                int voltage_uV;
+                try (java.io.BufferedReader r = new java.io.BufferedReader(
+                        new java.io.FileReader(volt))) {
+                    String s = r.readLine();
+                    if (s == null) {
+                        Log.w(TAG, "getRealChargingPower: voltage_now null");
+                        return 0;
+                    }
+                    voltage_uV = Integer.parseInt(s.trim());
+                } catch (Exception e) {
+                    Log.e(TAG, "getRealChargingPower: read voltage failed", e);
+                    return 0;
+                }
+                // Sign convention: gauge returns negative when charging.
+                // Power display wants positive wattage when charging in.
+                int current_uA = -current_mA * 1000;
+                Log.i(TAG, "getRealChargingPower: gauge=" + current_mA
+                        + "mA voltage=" + voltage_uV + "uV");
+                if (current_uA <= 0) {
+                    return 0;
+                }
+                // µW = (µA × µV) / 1e6
+                int uw = (int) ((long) current_uA * voltage_uV / 1_000_000L);
+                Log.i(TAG, "getRealChargingPower: returning " + uw + "uW");
+                return uw;
+            } catch (Exception e) {
+                Log.e(TAG, "getRealChargingPower: outer exception", e);
+                return 0;
+            }
+        }
+
         @Override
         public boolean isFastChargeSupported() {
             return mFCC.isSupported();
diff --git a/lineage/lib/main/java/org/lineageos/platform/internal/health/Util.java b/lineage/lib/main/java/org/lineageos/platform/internal/health/Util.java
index 1486467..31ae807 100644
--- a/lineage/lib/main/java/org/lineageos/platform/internal/health/Util.java
+++ b/lineage/lib/main/java/org/lineageos/platform/internal/health/Util.java
@@ -6,7 +6,12 @@
 package org.lineageos.platform.internal.health;
 
 import android.content.Context;
+import android.os.IBinder;
+import android.os.Parcel;
+import android.os.RemoteException;
+import android.os.ServiceManager;
 import android.text.format.DateFormat;
+import android.util.Log;
 
 import java.text.SimpleDateFormat;
 import java.time.LocalDate;
@@ -55,6 +60,95 @@ public class Util {
      * @param time seconds of the day
      * @return UTC milliseconds from epoch
      */
+    /**
+     * Engage real OPLUS bypass via the V10 vendor charger AIDL.
+     * setChgConfig(flag=25, "1+switch=1"|"1+switch=0", caller=2) — same call OOS
+     * Battery.apk's BypassChargeController makes. Hand-rolled Parcel to avoid
+     * pulling in the OEM-versioned (127-method) Java AIDL stub.
+     *
+     * Direct sysfs writes (PLC, mmi_charging_enable, chg_path) all fail to
+     * actually open the battery FET — only this binder path does, because the
+     * V10 service coordinates the charge-pump + FET + USB negotiation atomically.
+     *
+     * Returns true if the AIDL call returned 0 (success); false on any error
+     * (HAL not present, transact failed, exception). Always safe to call —
+     * silently no-ops on devices without the OPLUS HAL.
+     */
+    public static boolean callOplusBypassAidl(boolean enable) {
+        IBinder binder = ServiceManager.getService(
+                "vendor.oplus.hardware.charger.ICharger/default");
+        if (binder == null) {
+            Log.w("LineageHealth", "OPLUS charger HAL not available");
+            return false;
+        }
+        Parcel data = Parcel.obtain();
+        Parcel reply = Parcel.obtain();
+        try {
+            data.writeInterfaceToken("vendor.oplus.hardware.charger.ICharger");
+            data.writeInt(25);                                   // ChgFuncFlag = bypass
+            data.writeString(enable ? "1+switch=1" : "1+switch=0");
+            data.writeInt(2);                                    // caller = matches OOS
+            if (!binder.transact(127, data, reply, 0)) {        // TRANSACTION_setChgConfig
+                Log.e("LineageHealth", "OPLUS setChgConfig transact returned false");
+                return false;
+            }
+            reply.readException();
+            int result = reply.readInt();
+            Log.i("LineageHealth", "OPLUS setChgConfig(25, " + (enable ? "ON" : "OFF")
+                    + ", 2) = " + result);
+            return result == 0;
+        } catch (RemoteException | RuntimeException e) {
+            Log.e("LineageHealth", "OPLUS setChgConfig failed", e);
+            return false;
+        } finally {
+            reply.recycle();
+            data.recycle();
+        }
+    }
+
+    /**
+     * Engage OPLUS "Smart Charge Protection" — the declarative charge-limit
+     * feature exposed by the V10 charger HAL. Tell the service the level,
+     * the service holds the line autonomously (no polling required from us).
+     *
+     * Reverse-engineered from OOS Battery.apk SmartChargeProtectionController:
+     * setChgConfig(flag=14, "2++<switch>+<level>", caller=2).
+     *
+     * Use this for MODE_LIMIT instead of repeatedly toggling the bypass flag —
+     * bypass (flag 25) silently no-ops at high SoC, smart-protection doesn't.
+     */
+    public static boolean callOplusSmartLimit(boolean enable, int levelPct) {
+        IBinder binder = ServiceManager.getService(
+                "vendor.oplus.hardware.charger.ICharger/default");
+        if (binder == null) {
+            Log.w("LineageHealth", "OPLUS charger HAL not available");
+            return false;
+        }
+        Parcel data = Parcel.obtain();
+        Parcel reply = Parcel.obtain();
+        try {
+            data.writeInterfaceToken("vendor.oplus.hardware.charger.ICharger");
+            data.writeInt(14);                                   // ChgFuncFlag = smart-limit
+            data.writeString("2++" + (enable ? "1" : "0") + "+" + levelPct);
+            data.writeInt(2);                                    // caller
+            if (!binder.transact(127, data, reply, 0)) {
+                Log.e("LineageHealth", "OPLUS smart-limit transact returned false");
+                return false;
+            }
+            reply.readException();
+            int result = reply.readInt();
+            Log.i("LineageHealth", "OPLUS smartLimit("
+                    + (enable ? "ON@" + levelPct : "OFF") + ") = " + result);
+            return result == 0;
+        } catch (RemoteException | RuntimeException e) {
+            Log.e("LineageHealth", "OPLUS smart-limit failed", e);
+            return false;
+        } finally {
+            reply.recycle();
+            data.recycle();
+        }
+    }
+
     static public long getTimeMillisFromSecondOfDay(int time) {
         ZoneId utcZone = ZoneOffset.UTC;
         LocalDate currentDate = LocalDate.now();
diff --git a/lineage/lib/main/java/org/lineageos/platform/internal/health/ccprovider/Toggle.java b/lineage/lib/main/java/org/lineageos/platform/internal/health/ccprovider/Toggle.java
index a66e77c..ae8922c 100644
--- a/lineage/lib/main/java/org/lineageos/platform/internal/health/ccprovider/Toggle.java
+++ b/lineage/lib/main/java/org/lineageos/platform/internal/health/ccprovider/Toggle.java
@@ -21,6 +21,7 @@ import android.os.BatteryUsageStats;
 import android.util.Log;
 
 import org.lineageos.platform.internal.R;
+import org.lineageos.platform.internal.health.Util;
 
 import vendor.lineage.health.ChargingControlSupportedMode;
 import vendor.lineage.health.IChargingControl;
@@ -97,6 +98,10 @@ public class Toggle extends ChargingControlProvider {
         mIsLimitSet = shouldStopCharging(currentPct, targetPct);
         Log.i(TAG, "Current battery level: " + currentPct + ", target: " + targetPct +
                 ", limit set: " + mIsLimitSet);
+        // Declare the limit to OPLUS's Smart Charge Protection. Once told the
+        // level, the service holds it autonomously — the bypass-style flag 25
+        // silently no-ops at high SoC but flag 14 (smart-limit) is sticky.
+        Util.callOplusSmartLimit(mIsLimitSet, targetPct);
         return setChargingEnabled(!mIsLimitSet);
     }
 
@@ -197,17 +202,25 @@ public class Toggle extends ChargingControlProvider {
     }
 
     private boolean setChargingEnabled(boolean enabled) {
+        // Engage real bypass via OPLUS vendor charger HAL. The Lineage HAL
+        // only writes to /sys/class/oplus_chg/common/plc which the kernel
+        // accepts but doesn't act on (verified empirically). Calling the
+        // OPLUS AIDL is the only path that actually opens the battery FET.
+        // setChargingEnabled(false) → bypass ON; (true) → bypass OFF.
+        boolean ok = Util.callOplusBypassAidl(!enabled);
+
         try {
             if (mToggleSetAlways) {
                 mChargingControl.setChargingEnabled(enabled);
             } else if (mChargingControl.getChargingEnabled() != enabled) {
                 mChargingControl.setChargingEnabled(enabled);
             }
-            return true;
         } catch (Exception e) {
-            Log.e(TAG, "Failed to set charging enabled", e);
-            return false;
+            Log.w(TAG, "Lineage HAL setChargingEnabled threw, ignoring: "
+                    + e.getMessage());
         }
+
+        return ok;
     }
 
     @Override
@@ -223,6 +236,9 @@ public class Toggle extends ChargingControlProvider {
     @Override
     protected void onReset() {
         try {
+            // Release any active OPLUS smart-limit. Level is irrelevant when
+            // disabling.
+            Util.callOplusSmartLimit(false, 100);
             mChargingControl.setChargingEnabled(true);
             mIsLimitSet = false;
             mSavedTargetTime = 0;
diff --git a/sdk/src/java/lineageos/health/HealthInterface.java b/sdk/src/java/lineageos/health/HealthInterface.java
index 40f438a..be36032 100644
--- a/sdk/src/java/lineageos/health/HealthInterface.java
+++ b/sdk/src/java/lineageos/health/HealthInterface.java
@@ -284,6 +284,118 @@ public class HealthInterface {
         }
     }
 
+    /**
+     * Returns whether bypass charging (battery FET held open while USB powers
+     * the system) is supported by the underlying HAL.
+     */
+    public boolean isBypassChargingSupported() {
+        try {
+            return checkService() && sService.isBypassChargingSupported();
+        } catch (RemoteException e) {
+            return false;
+        }
+    }
+
+    /**
+     * Returns the manual bypass-charging override state. Independent of
+     * {@link #getEnabled()} — when true, the device forces bypass regardless of
+     * the regular Charging Control logic.
+     */
+    public boolean getBypassChargingEnabled() {
+        try {
+            return checkService() && sService.getBypassChargingEnabled();
+        } catch (RemoteException e) {
+            return false;
+        }
+    }
+
+    /**
+     * Toggle the manual bypass-charging override.
+     */
+    public boolean setBypassChargingEnabled(boolean enabled) {
+        try {
+            return checkService() && sService.setBypassChargingEnabled(enabled);
+        } catch (RemoteException e) {
+            return false;
+        }
+    }
+
+    /**
+     * Returns whether the kernel has currently engaged bypass charging
+     * (battery FET held open, USB powering system). Reflects ground truth
+     * from sysfs, not user intent — so flipping {@link #setBypassChargingEnabled}
+     * to true does NOT immediately make this return true; it depends on the
+     * HAL successfully writing PLC and the kernel acknowledging.
+     */
+    public boolean isBypassChargingActive() {
+        try {
+            return checkService() && sService.isBypassChargingActive();
+        } catch (RemoteException e) {
+            return false;
+        }
+    }
+
+    /**
+     * Whether True Bypass charging is supported on this device. Requires the
+     * OPLUS Smart Charge Protection AIDL (charger HAL flag 14) to be present.
+     */
+    public boolean isTrueBypassChargingSupported() {
+        try {
+            return checkService() && sService.isTrueBypassChargingSupported();
+        } catch (RemoteException e) {
+            return false;
+        }
+    }
+
+    /**
+     * Returns the True Bypass user intent (Settings value), independent of
+     * whether it's currently engaged.
+     */
+    public boolean getTrueBypassChargingEnabled() {
+        try {
+            return checkService() && sService.getTrueBypassChargingEnabled();
+        } catch (RemoteException e) {
+            return false;
+        }
+    }
+
+    /**
+     * Toggle True Bypass user intent. Engagement also depends on Normal Bypass
+     * being enabled and battery > 20%; auto-disables at the threshold.
+     */
+    public boolean setTrueBypassChargingEnabled(boolean enabled) {
+        try {
+            return checkService() && sService.setTrueBypassChargingEnabled(enabled);
+        } catch (RemoteException e) {
+            return false;
+        }
+    }
+
+    /**
+     * True iff user has both Normal and True Bypass enabled, USB is plugged,
+     * and battery is above the 20% safety threshold. Used by the AOD label
+     * + the persistent notification to know what to display.
+     */
+    public boolean isTrueBypassChargingActive() {
+        try {
+            return checkService() && sService.isTrueBypassChargingActive();
+        } catch (RemoteException e) {
+            return false;
+        }
+    }
+
+    /**
+     * Real-time charging power in microwatts (battery current × voltage).
+     * Returns 0 if not charging or unavailable.
+     */
+    public int getRealChargingPowerMicrowatts() {
+        try {
+            return checkService() ? sService.getRealChargingPowerMicrowatts() : 0;
+        } catch (RemoteException e) {
+            return 0;
+        }
+    }
+
     /**
      * Returns whether fast charge is supported
      *
diff --git a/sdk/src/java/lineageos/health/IHealthInterface.aidl b/sdk/src/java/lineageos/health/IHealthInterface.aidl
index 633f104..675047d 100644
--- a/sdk/src/java/lineageos/health/IHealthInterface.aidl
+++ b/sdk/src/java/lineageos/health/IHealthInterface.aidl
@@ -31,4 +31,44 @@ interface IHealthInterface {
     int[] getSupportedFastChargeModes();
     int getFastChargeMode();
     boolean setFastChargeMode(int mode);
+
+    // Manual bypass-charging override. Appended at the end on purpose — AIDL
+    // transaction codes are positional, so adding new methods anywhere except
+    // the tail breaks ABI for already-built clients.
+    boolean isBypassChargingSupported();
+    boolean getBypassChargingEnabled();
+    boolean setBypassChargingEnabled(boolean enabled);
+    /**
+     * Returns whether the kernel has *currently* engaged the PLC bypass path
+     * (status=3 in /sys/class/oplus_chg/common/plc), independent of the
+     * user's request flag. Used by SystemUI for the AOD/lockscreen label so
+     * it reflects ground truth rather than user intent — if the user enabled
+     * bypass but the kernel didn't engage it (PLC subsystem race etc.), the
+     * label flips back to plain "Charging" so the user notices.
+     *
+     * Returns false on devices that don't expose the PLC sysfs node.
+     */
+    boolean isBypassChargingActive();
+
+    // True Bypass charging — fully opens the battery FET via OPLUS Smart
+    // Charge Protection (flag 14). Builds on top of normal Bypass; auto-
+    // disables at ≤ 20% SoC and stays off until user re-enables.
+    boolean isTrueBypassChargingSupported();
+    boolean getTrueBypassChargingEnabled();
+    boolean setTrueBypassChargingEnabled(boolean enabled);
+    /**
+     * Returns whether True Bypass is *currently* engaged (USB plugged AND
+     * battery > 20% safety threshold AND user has both Normal and True
+     * Bypass enabled). Reflects ground truth.
+     */
+    boolean isTrueBypassChargingActive();
+
+    /**
+     * Real-time charging power in microwatts, computed from the OPLUS gauge
+     * MMS interface (battery current × voltage). Reflects what the cable
+     * power meter would show. Returns 0 if the device isn't charging or the
+     * gauge can't be read. Used by the lock screen "show real power" toggle
+     * to override the default adapter-rating display.
+     */
+    int getRealChargingPowerMicrowatts();
 }
diff --git a/sdk/src/java/lineageos/providers/LineageSettings.java b/sdk/src/java/lineageos/providers/LineageSettings.java
index 118251b..3c40c5a 100644
--- a/sdk/src/java/lineageos/providers/LineageSettings.java
+++ b/sdk/src/java/lineageos/providers/LineageSettings.java
@@ -1432,6 +1432,51 @@ public final class LineageSettings {
         public static final Validator CHARGING_CONTROL_LIMIT_VALIDATOR =
                 new InclusiveIntegerRangeValidator(70, 100);
 
+        /**
+         * Manual bypass-charging override. When 1, the device runs from external
+         * power while the battery FET is held open (no charge / no discharge).
+         * Independent of CHARGING_CONTROL_ENABLED — when set, takes priority over
+         * the auto Toggle/Limit logic, so flipping this on overrides whatever the
+         * Charging Control framework would otherwise do.
+         * The value is boolean (1 or 0).
+         */
+        public static final String BYPASS_CHARGING_ENABLED = "bypass_charging_enabled";
+
+        /** @hide */
+        public static final Validator BYPASS_CHARGING_ENABLED_VALIDATOR =
+                sBooleanValidator;
+
+        /**
+         * Whether True Bypass charging is enabled. Builds on top of normal
+         * Bypass — when true, drives the OPLUS Smart Charge Protection AIDL
+         * (flag 14) to fully open the battery FET so USB powers the system
+         * directly. Normal Bypass (flag 25, "halve mode") must also be
+         * enabled for True Bypass to engage. Auto-disables at ≤ 20% SoC and
+         * stays off until user re-enables.
+         * The value is boolean (1 or 0).
+         */
+        public static final String TRUE_BYPASS_CHARGING_ENABLED =
+                "true_bypass_charging_enabled";
+
+        /** @hide */
+        public static final Validator TRUE_BYPASS_CHARGING_ENABLED_VALIDATOR =
+                sBooleanValidator;
+
+        /**
+         * Whether the lock screen charging indicator should show real-time
+         * power flow (computed from battery/current_now × voltage_now)
+         * instead of the adapter's rated capability. Default off — matches
+         * stock AOSP behaviour, which displays the negotiated charger
+         * capability ("80 W charger") rather than instantaneous draw.
+         * The value is boolean (1 or 0).
+         */
+        public static final String SHOW_REAL_CHARGING_POWER =
+                "show_real_charging_power";
+
+        /** @hide */
+        public static final Validator SHOW_REAL_CHARGING_POWER_VALIDATOR =
+                sBooleanValidator;
+
         /**
          * Fast charging mode
          */
@@ -2212,6 +2257,11 @@ public final class LineageSettings {
             VALIDATORS.put(CHARGING_CONTROL_START_TIME, CHARGING_CONTROL_START_TIME_VALIDATOR);
             VALIDATORS.put(CHARGING_CONTROL_TARGET_TIME, CHARGING_CONTROL_TARGET_TIME_VALIDATOR);
             VALIDATORS.put(CHARGING_CONTROL_LIMIT, CHARGING_CONTROL_LIMIT_VALIDATOR);
+            VALIDATORS.put(BYPASS_CHARGING_ENABLED, BYPASS_CHARGING_ENABLED_VALIDATOR);
+            VALIDATORS.put(TRUE_BYPASS_CHARGING_ENABLED,
+                    TRUE_BYPASS_CHARGING_ENABLED_VALIDATOR);
+            VALIDATORS.put(SHOW_REAL_CHARGING_POWER,
+                    SHOW_REAL_CHARGING_POWER_VALIDATOR);
             VALIDATORS.put(FAST_CHARGE_MODE, FAST_CHARGE_MODE_VALIDATOR);
             VALIDATORS.put(BATTERY_LIGHT_ENABLED, BATTERY_LIGHT_ENABLED_VALIDATOR);
             VALIDATORS.put(BATTERY_LIGHT_FULL_CHARGE_DISABLED,
